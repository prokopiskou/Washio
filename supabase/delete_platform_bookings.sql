-- ============================================================
-- Διαγραφή ΜΟΝΟ των test κρατήσεων της ΠΛΑΤΦΟΡΜΑΣ (app: κάρτα + μετρητά).
-- ΔΕΝ αγγίζει τις χειροκίνητες κρατήσεις των πλυντηρίων (source = 'manual').
-- ============================================================

-- 1) (Προαιρετικό) Δες πόσες θα σβηστούν πριν τρέξεις τη διαγραφή:
select count(*) as platform_bookings_to_delete
from public.bookings
where source = 'platform';

-- 2) Η διαγραφή:
delete from public.bookings
where source = 'platform';

-- (Οι χειροκίνητες, source = 'manual', παραμένουν ανέπαφες.)
